{
  "description": "Describes and defines completion for functions/snippets usually called from the global card table (`cXXXXXXXX` or `s`).",
  "type": "array",
  "items": {
    "type": "object",
    "required": [ "id", "desc" ],
    "properties": {
      "id": { "type": "string" },
      "args": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [ "id", "type" ],
          "properties": {
            "id": { "type": "string" },
            "type": { "type": "string" },
            "opt": { "type": "boolean" }
          }
        }
      },
      "ret": {
        "type": "array",
        "items": { "type": "string" }
      },
      "desc": { "type": "string" }
    }
  }
}
