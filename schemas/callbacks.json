{
  "description": "Defines snippets for different kinds of callback functions used by card effects. E.g. `initial_effect`, `operation`, `condition`, etc.",
  "type": "array",
  "items": {
    "type": "object",
    "required": [ "id", "desc" ],
    "properties": {
      "id": { "type": "string" },
      "args": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [ "id", "type" ],
          "properties": {
            "id": { "type": "string" },
            "type": { "type": "string" },
            "opt": { "type": "boolean" }
          }
        }
      },
      "ret": {
        "type": "array",
        "items": { "type": "string" }
      },
      "desc": { "type": "string" },
      "uses": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
